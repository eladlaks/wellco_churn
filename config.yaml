web_feats:
  use_custom_features_web: true
  use_domain: true
threshold: 0.5
# Uplift modeling method. Options:
#  - two_model: train separate models for treatment and control (default)
#  - s_learner: train a single model with the treatment indicator as a feature
uplift_method: s_learner
dates:
  treatment_date: "14/07/2025"
  window_len: 14